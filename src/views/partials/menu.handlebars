
<nav class="menu">
    {{#if user}}
    {{#if (or (eq user.role 'admin') (eq user.role 'premium'))}}
    <a href="/realtimeproducts" class="menu-item">Productos en Tiempo Real</a>
    <span class="separator">|</span>
    {{/if}}

    {{#if (or (eq user.role 'user') (eq user.role 'premium'))}}
    <a href="/products" class="menu-item">Tienda</a>
    <span class="separator">|</span>
    <a href="/chat" class="menu-item">Chat en Vivo</a>
    <span class="separator">|</span>
    {{/if}}

    <a href="/profile" class="menu-item">Perfil</a>
    <span class="separator">|</span>

    {{#if (eq user.role 'admin')}}
    <a href="/admin/users" class="menu-item">Roles de Usuarios</a>
    <span class="separator">|</span>
    {{/if}}

    <a href="/logout" class="menu-item">Cerrar Sesión</a>
    {{/if}}
</nav>